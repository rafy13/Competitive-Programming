#include<stdio.h>
#include<iostream>
#include<string.h>
#include<stdlib.h>
using namespace std;
int main()
{
    int n,i,j;
    char p[130];
    cin>>n;
    for(i=0;i<n;i++)
    {
        j=0;
        while(if(cin>>p[j]!)=EOF)
        {
            if(j==0)
                if(p[j]==')'||p[j]==']')
            {
                printf("No\n");
                f=0;
                break;
            }
           else if(p[j==')')
           {
               if(p[j-1]!=)
           }
        }
    }
}
