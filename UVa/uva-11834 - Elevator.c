#include<stdio.h>
#include<math.h>
#include<iostream>
using namespace std;
int main()
{
    int l,b,p,q,s;
    while(1)
    {
        cin>>l>>b>>p>>q;
        if(l==0&&b==0&&p==0&&q==0)
            break;
        if(l>b)
            s=b;
        else=l;
        if(2*p>s&&2*q>s&&(sqrt(8)*(p+q))>sqrt(a*a+b*b))
            cout<<"S\n";
        else
            cout<<"N\n";

    }
    return 0;
}
