#include<stdio.h>
int main()
{
    int n,i,j,a,b,tp;
    scanf("%d",n);
    int p[n];
    for(i=0;i<n;i++)
        scanf("&d",&p[i]);
    for(i=0;i<n;i++)
    {
        tp=p[i];
        while((tp+p[i])/2!=p[i])
        {
            ps=
        }
    }
}
